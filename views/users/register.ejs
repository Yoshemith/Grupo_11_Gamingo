<!DOCTYPE html>
<html lang="es">
    <%- include('../partials/head.ejs') %>
    <link rel="stylesheet" href="/css/register.css">
<body>
    <%- include('../partials/header.ejs') %>

    <main class="main-login">
        <div class="botonesCrearIniciar">
            <div class="iniciar">
                <a href="/login">Iniciar sesión</a>
            </div>
            <div class="crear">
                <a href="/register">Crear cuenta</a>
            </div>
        </div>
        <div class="formularios">      
            <div class="form2"> 
                <form action="/register" method="POST" enctype="multipart/form-data" class="register-form">
                    <div class="input-form">
                        <label>Imagen de Perfil*</label>
                        <input type="file" name="imagenUsuario">
                    </div>
                    <div class="input-form">
                        <label for="nombre">Nombre(s)*</label>
                        <input type="text" name="nombre" id="nombre" placeholder="Nombre" value="<%= locals.old ? old.nombre : null %>" required>
                        <% if(locals.errors && errors.nombre){  %>
                            <div class="errores">
                                <p><%= errors.nombre.msg %></p>
                            </div>
                        <% } %>
                    </div>
                    <div class="input-form">
                        <label for="apellido">Apellidos*</label>
                        <input type="text" name="apellido" id="apellido" placeholder="Apellidos" value="<%= locals.old ? old.apellido : null %>" required>
                        <% if(locals.errors && errors.apellido){  %>
                            <div class="errores">
                                <p><%= errors.apellido.msg %></p>
                            </div>
                        <% } %>
                    </div>
                    <div class="input-form">
                        <label for="correo">Correo Electrónico*</label>
                        <input type="text" name="correo" id="correo" placeholder="tu@correo.com" value="<%= locals.old ? old.correo : null %>" required>
                        <% if(locals.errors && errors.correo){  %>
                            <div class="errores">
                                <p><%= errors.correo.msg %></p>
                            </div>
                        <% } %>
                    </div>
                    <div class="input-form">
                        <label for="contraseña">Contraseña*</label>
                        <input type="password" name="contraseña" id="contraseña" placeholder="Ingresa tu contraseña" required>
                        <div class="eye">
                            <i class="fas fa-eye" onclick="funcionContraseña()"></i>
                        </div>
                        <% if(locals.errors && errors.contraseña){  %>
                            <div class="errores">
                                <p><%= errors.contraseña.msg %></p>
                            </div>
                        <% } %>
                    </div>
                    <div class="input-form">
                        <div class="campos">
                            <p>*Campos requeridos</p>
                        </div>
                    </div>
                    <div class="input-form">
                        <div class="campos2">
                            <p>Al dar click en Crear Cuenta, estás aceptando los <a href="/terminos">Términos y Condiciones de Uso</a> de éste sitio. Adicionalmente estás aceptando nuestro <a href="/aviso">Aviso de Privacidad</a> y recibirás información publicitaria de Gamingo a tu correo electrónico.</p>
                        </div>
                    </div>
                    <div class="input-form">
                        <button type="submit">CREAR CUENTA</button>
                    </div>
                </form>
            </div>
        </div>
    </main>
    <%- include('../partials/footer.ejs') %>
    <script src="/js/registerValidation.js" type="text/javascript"></script>
</body>
</html>